version: "3"
services:
  nginx:
    container_name: nginx
    build: ./nginx
    restart: always
    depends_on:
      - api
    ports:
      - "3000:80"
  api:
    container_name: api
    build: ./api
    volumes:
      - /app/node_modules
      - ./api:/app
    command: npm run dev
